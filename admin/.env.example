# admin/.env.example — 복사해서 admin/.env 로 만들고 값 채우기
# (admin/.env 는 .gitignore 로 커밋 제외. ADMIN_SECRET 은 절대 공유 금지)
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_ANON_KEY=sb_publishable_...            # Supabase publishable(anon) key
ADMIN_SECRET=<db/schema.sql 에서 정한 운영자 토큰>
